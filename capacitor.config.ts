import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.jmfiebak.back2back',
  appName: 'Back2Back',
  webDir: 'dist/back2back',
};

export default config;
